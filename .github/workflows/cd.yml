# Continuous Deployment (CD) Workflow
# ===================================
#
# Author: Akshay Mestry <xa@mes3.dev>
# Created on: 25 April, 2026
# Last updated on: 25 April, 2026

name: Continuous Deployment
run-name: Started ${{ github.workflow }} (CD) workflow

on:
  workflow_dispatch:
    inputs:
      deploy_target:
        description: "Deployment environment"
        required: false
        default: "production"

env:
  ARTIFACT_NAME: webpages
  OUTPUT_DIR: docs/build/
  SOURCE_DIR: docs/source/
  PYTHON_VERSION: "3.13.7"

permissions:
  contents: write

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build website pages
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Setup Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ env.PYTHON_VERSION }}
      - name: Install Kaamiki theme
        run: python -m pip install -Uq git+https://github.com/xames3/website.git
      - name: Build HTML pages with Sphinx
        run: sphinx-build --builder dirhtml --fresh-env --quiet $SOURCE_DIR $OUTPUT_DIR
      - name: Upload built documentation as artifact
        uses: actions/upload-pages-artifact@v3
        with:
          name: ${{ env.ARTIFACT_NAME }}
          path: ${{ env.OUTPUT_DIR }}

  deploy:
    name: Deploy built website
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pages: write
      id-token: write
    environment:
      name: production
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
        with:
          artifact_name: ${{ env.ARTIFACT_NAME }}
